import javafx.geometry.Rectangle2D;

public class Hero  extends AnimatedThing{

    // Constructor
    public Hero(double x, double y, int attitude, int index, int maxIndex,double sizeX, double sizeY, String filename) {
        super(x, y, attitude, index, maxIndex, sizeX, sizeY, filename);
    }

}